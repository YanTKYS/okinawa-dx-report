# 沖縄県内11市 DX推進体制・DX推進状況 調査

沖縄県内の全11市を対象に、**DX・業務改善を担う人的体制**と**実際のDX推進状況**の関係を検証する調査リポジトリ。

## 検証する仮説

> DX推進部署・業務改善部署など、DXを専門的に推進する組織の人的体制が充実している自治体ほど、DX推進が進んでいるのではないか

**結論ありきではない。** 正の関係・無関係・負の関係のいずれもあり得るものとして検証している。

## 現在のステータス（2026年9月16日）

**仮説は現時点では判定できない。** ただし人員側のデータは大きく前進した。

| 項目 | 状態 |
|---|---|
| DX・情報関係業務担当職員数（総務省・広義） | **8/11市 確定（確度A）** 同一定義・同一基準日2024-04-01 |
| 狭義のDX／BPR／情報システム運用人数 | 3市（那覇市・宜野湾市・浦添市） |
| DX推進度スコア | **11/11市 採点済**（凍結ルーブリック100点満点） |
| ただし未確認率 | **全11市が80%超** → 凍結ルールにより主分析は対象0市 |
| 相関分析（主分析） | n=0（未確認率30%超で全市除外） |
| 相関分析（感度分析・参考値） | n=8 / Pearson r = -0.174 / Spearman ρ = -0.276（**有意でない**） |
| 人口・総職員数 | 人口1/11市、総職員数0/11市 → 人口補正・職員数補正は**算出不能** |

**ボトルネックは人員側からDX推進度側へ移動した。**
次の優先作業は、内部業務DX（確認密度0.0%）・住民サービス（0.0%）・行政手続DX（1.1%）の一次資料確認。

> 感度分析の弱い負の値を「人員が多いほどDXが遅れている」と読んではならない。
> この値はDX推進度の実態ではなく**調査の到達度**を反映している
> （人員最多の名護市は未確認率95%、2位の沖縄市は100%）。

## 閲覧用ページ（GitHub Pages）

<https://yantkys.github.io/okinawa-dx-report/>

CSVやレポートを個別に開かなくても、**`index.html` を開くだけ**で
11市の人的体制・DX推進度・両者の関係・糸満市の相対的位置・分析の限界を数分で把握できる。

- **ローカル確認**：リポジトリのルートで `python3 -m http.server 8000` を実行し <http://localhost:8000/> を開く
  （`file://` で直接開くとブラウザのセキュリティ制限によりCSVを読み込めない）
- ページは **`data/` 配下のCSVを読み込んで描画**しており、数値をHTMLに直接書き込んでいない。
  **CSVを更新すれば表示・集計・相関分析も自動的に更新される。**
- 静的HTML/CSS/JavaScriptのみ。ビルド処理・フレームワーク・外部CDNに依存しない。印刷／PDF保存に対応。

## ⚠️ 人員指標の二層構造（最重要）

本リポジトリは**定義の異なる2種類の人員指標**を別列で管理している。混同してはならない。

| 列 | 意味 | 用途 |
|---|---|---|
| `dx_info_staff` | 総務省「自治体DX・情報化推進概要」の**DX推進担当課室・情報政策担当課室の合計人数**（2024-04-01基準）。**情報政策担当を含む広義値でありDX専任人数ではない** | **主分析**。11市を同一定義で比較できる唯一の共通指標 |
| `dx_staff` | DX企画・BPR・業務改善等を担う職員数。**情報システム運用と分離**（methodology §7.2） | 補助分析。現在2市のみ |
| `dx_bpr_combined_staff` | DXとBPRが一体で分離不能な組織の総数（浦添市 行政改革推進課5人） | **按分禁止** |

- **総務省の広義値を `dx_staff` に入力してはならない。**
- UI・レポートでは広義指標を「**DX・情報関係業務担当職員数**」と表示し、「DX担当人数」とは表示しない。

## ファイル構成

```
index.html                    閲覧用ページ（GitHub Pages のエントリポイント）
assets/style.css              スタイル（印刷用レイアウトを含む）
assets/app.js                 CSV読み込み・集計・相関分析・描画
docs/
  okinawa-city-dx-report.md   最終レポート本体（13章構成）
  methodology.md              調査設計・評価基準（2026-09-15凍結）＋§12 追加指標の追記
  sources.md                  出典一覧・一次資料URL対応表
data/
  municipalities.csv          市別データセット（45列）。未確定値は「不明」
  scoring_results.csv         DX推進度の採点根拠（11市×21項目＝231行）
  analysis_results.csv        相関分析結果のスナップショット
  evidence.csv                データ1件ごとの根拠・確度・検証状態（95行）
  scoring_rubric.csv          評価ルーブリック（6軸21項目100点・凍結）
  deep-research-*.md          3回のDeep Research 生記録
tools/
  build_analysis.py           採点・派生列・相関分析の生成（再実行可能）
  merge_evidence.py           Deep Research 根拠の evidence.csv 統合
  test_checks.py              データ品質チェック34項目
```

## データ取り扱いの原則

- **`不明`・`未評価` を 0 として計算しない。** 欠損は集計・相関から除外し、除外数を必ず表示する
- **推測値で補完しない。** 総務省値が取得できない3市は欠損のまま
- **「情報が見つからない」を「実施していない」と判定しない。** 未確認フラグで区別する
- 確度A・Bの一次資料根拠がある項目のみ採点する（確度Cはスコアに反映しない）
- 評価基準は2026-09-15に凍結。**結果に合わせて変更しない**

## 使い方（調査を完了させる手順）

1. **DX推進度の未確認項目を埋める（最優先）。** `data/scoring_results.csv` の
   `status` が `未確認` の行について一次資料を確認し、`points`／`confidence`／`source_url`／`evidence` を更新
2. **人口・普通会計職員数を11市同一定義で取得**し `municipalities.csv` に入力
3. **総務省資料 Excel 1753／1754／1756行**から那覇市・宜野湾市・浦添市の `dx_info_staff` を取得
4. `python3 tools/build_analysis.py` を実行して採点集計・派生列・相関分析を再生成
5. `python3 tools/test_checks.py` でデータ品質を検証
6. ページを再読み込みすると、散布図・相関係数・補正分析が自動的に更新される（HTMLの修正は不要）

**確認できない値は「不明」のままにすること。0 を入れてはならない。**

## 調査時点

人員データ基準日：2024年4月1日（総務省調査）
DX推進度 評価時点：2026年9月15日
本分析は **2024年度の人員体制と約2年後のDX推進状況を比較するラグ分析**であり、同時点比較ではない。
逆因果の影響を多少避けられる可能性はあるが、**因果関係を証明するものではない。**
